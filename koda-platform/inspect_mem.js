const { createClient } = require('@supabase/supabase-js'); const dotenv = require('dotenv'); dotenv.config(); const supabase = createClient(process.env.SUPABASE_URL, process.env.SUPABASE_SERVICE_ROLE_KEY); async function inspectMem() { const { data, error } = await supabase.from('memories').select('*').limit(5); if (error) console.error(error); else console.log(data); } inspectMem();
